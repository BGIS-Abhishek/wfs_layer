# wfs_layer

A Flutter package to load and display Web Feature Service (WFS) layers with custom styling and clustering support.

## Installation

Add the following to your `pubspec.yaml`:

```yaml
dependencies:
  wfs_layer: ^0.0.1